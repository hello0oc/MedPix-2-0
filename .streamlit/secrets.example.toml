# MedGUI credentials template.
# Copy to `.streamlit/secrets.toml` and fill in real values.
# Never commit secrets.toml — it is listed in .gitignore.

HF_TOKEN = "your_huggingface_token"
GEMINI_API_KEY = "your_gemini_api_key"
